# Error codes

!!! info "Auto-generated reference — not yet wired up"
    This page will be generated at build time from the installed
    `scrolly` error catalog, so it can't drift from the code. Until
    that generation step is in place, use the CLI as the source of
    truth:

    ```bash
    scrolly errors --list-codes
    scrolly errors <code>
    ```

Validation and parse errors carry numbered codes (e.g. `[E202]`); look
up any code's cause, example, and fix with `scrolly errors <code>`.
