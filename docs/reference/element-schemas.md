# Element schemas

!!! info "Auto-generated reference — not yet wired up"
    This page will be generated at build time from the installed
    `scrolly` version, so the per-element field lists can't drift from
    the code. Until that generation step is in place, use the CLI as
    the source of truth:

    ```bash
    scrolly schema --list-types
    scrolly schema <type>
    ```

For the element model and how schemas fit in, see
[Elements](../concepts/elements.md).
