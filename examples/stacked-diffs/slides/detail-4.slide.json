// Instantiation of the shared detail-slide factory — layout lives in
// ../factories/detail.slide.json.j2; only the content varies per slide.
{
  template_file: "../factories/detail.slide.json.j2",
  with: {
    heading: "Sliding out a merged branch",
    accent: "#9DD2BA",
    body: "## Notes\n\
\n\
* merging top-most PR requires 2 changes\n\
    * remove merged branch from git-machete state\n\
    * implied: retarget PR2 to `main`\n\
* `git machete slide-out` and `git machete traverse` take care of this efficiently\n\
* when GitHub merges PR1 and deletes the source branch, PR2 will automatically get retargeted to main (usually voiding any granted approval)\n\
    * improved GitHub support for stacked PRs under development",
  },
}
