// Instantiation of the shared detail-slide factory — layout lives in
// ../factories/detail.slide.json.j2; only the content varies per slide.
{
  template_file: "../factories/detail.slide.json.j2",
  with: {
    heading: "Rebase: stack behind main",
    accent: "#73BB96",
    body: "## Notes\n\
\n\
* `git machete traverse` fully orchestrates rebase of entire stack\n\
* when a rebase conflict occurs, git-machete bails out with detailed error message\n\
* conflict can then be resolved manually or using e.g. Claude Code / Codex\n\
* Agentic coding assistants excel at robustly executing full stack traverse using git-machete while resolving any rebase conflicts correctly along the way",
  },
}
