// Instantiation of the shared detail-slide factory — layout lives in
// ../factories/detail.slide.json.j2; only the content varies per slide.
{
  template_file: "../factories/detail.slide.json.j2",
  with: {
    heading: "Rebase: new commits across the stack",
    accent: "#88C6A8",
    body: "## Notes\n\
\n\
* this workflow does not only keep PRs nice and small, but also allows parallel implementation using git worktrees & parallel coding sessions\n\
* `git machete traverse` deals with these rebases just as well",
  },
}
