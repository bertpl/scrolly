// Instantiation of the shared detail-slide factory — layout lives in
// ../factories/detail.slide.json.j2; only the content varies per slide.
{
  template_file: "../factories/detail.slide.json.j2",
  with: {
    heading: "Building the stack",
    subtitle: "Details (1/2)",
    accent: "#5EB083",
    body: "## Notes\n\
\n\
* branches can be created using plain git\n\
* `.git/machete` is the only state git-machete keeps and can be hand-edited\n\
* alternatively, use `git machete discover` with appropriate flags to let git-machete auto-discover branch stack\n\
* creating PRs through git machete ensures target branches are correctly set and PR descriptions hold \"machete blocks\" for easy navigation through the PR stack",
  },
}
