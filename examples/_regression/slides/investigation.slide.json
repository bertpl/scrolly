// The investigation chain: anomaly → valgrind → IFUNC trail → disclosure.
{
  title: "Working backwards",
  scroll_range: 1390,
  // Start + end snap stops, merged with the image-sequence frame snaps.
  snap_positions: [0, 1390],
  elements: [
    {
      name: "caption",
      markdown: "## The investigation\n\nFour steps from anomaly to disclosure — each layer adds new evidence.",
      position: [50, 5], anchor: [50, 0],
      width: 90, height: "auto",
      text_align: "center",
      color: "#2c4d2c",
    },
    // Crossfade through the four investigation scenes — each a full
    // step from anomaly to disclosure.
    {
      name: "steps",
      image_sequence: [
        "../investigation-1.svg",
        "../investigation-2.svg",
        "../investigation-3.svg",
        "../investigation-4.svg",
      ],
      compositing: "overlay",
      frame_distance: 380,
      // hold_fraction omitted → uses the default (0.2).
      fade_in: 100,
      fade_out: 150,
      scroll_offset: 100,
      position: [50, 28], anchor: [50, 0],
      width: 75, height: "auto",
    },
  ],
}
