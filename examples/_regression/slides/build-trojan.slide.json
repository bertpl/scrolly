// The build-time staging — how the payload bytes became executable code.
{
  title: "Build-time staging",
  scroll_range: 1150,
  // Start + end snap stops, merged with the image-sequence frame snaps.
  snap_positions: [0, 1150],
  elements: [
    {
      name: "caption",
      markdown: "## The autoconf trojan\n\nThe payload was assembled at `./configure` time by a modified `build-to-host.m4` macro that decoded bytes from the test fixtures and stitched them into the compiled library.",
      position: [50, 5], anchor: [50, 0],
      width: 90, height: "auto",
      text_align: "center",
      color: "#5a2c2c",
    },
    // The trojan assembles stage by stage: each layer adds the next box,
    // stacking into the full flow. fade_out:0 holds the finished diagram
    // at the end (avoids the incremental shine-through of a trailing fade).
    {
      name: "assembly",
      image_sequence: [
        "../trojan-1.svg",
        "../trojan-2.svg",
        "../trojan-3.svg",
        "../trojan-4.svg",
      ],
      compositing: "incremental",
      frame_distance: 350,
      hold_fraction: 0.4,
      fade_in: 100,
      fade_out: 0,
      scroll_offset: 100,
      position: [50, 32], anchor: [50, 0],
      width: 80, height: "auto",
    },
  ],
}
