# E005 - Field type mismatch (list expected)

## Cause

A field that must be a list/array was given a different type.

## Example

```json5
{ slides: {} }   // 'slides' must be a list
```

## How to fix

Wrap the value in `[...]`, even if it's empty: `slides: []`.
